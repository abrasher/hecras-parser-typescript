import { schema } from "../schema"

/**
 * Placeholder plan schema. Implement the actual plan parsing/serialization logic
 * once the DSL migration for plan files begins.
 */
export const planSchema = schema([])
